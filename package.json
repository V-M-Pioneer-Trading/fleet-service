{
  "name": "fleet-service",
  "version": "1.0.0",
  "description": "Ship action commands (navigate, orbit, dock, extract, sell, etc.) for the SpaceTraders mining POC",
  "main": "dist/server.js",
  "scripts": {
    "build": "tsoa spec-and-routes && tsc",
    "start": "node dist/server.js",
    "dev": "npm run build && npm start",
    "pretest": "tsoa spec-and-routes",
    "test": "jest"
  },
  "dependencies": {
    "@tsoa/runtime": "^6.6.0",
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "swagger-ui-express": "^5.0.1"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^4.17.21",
    "@types/jest": "^29.5.14",
    "@types/node": "^22.10.5",
    "@types/supertest": "^6.0.2",
    "@types/swagger-ui-express": "^4.1.7",
    "jest": "^29.7.0",
    "supertest": "^6.3.4",
    "ts-jest": "^29.2.5",
    "tsoa": "^6.6.0",
    "typescript": "^5.7.3"
  }
}
